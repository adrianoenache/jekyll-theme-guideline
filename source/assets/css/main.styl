---
---
// See individual.styl below. Notice it has no YAML front matter.

{% for theme in site.data.theme %}
{{ theme.varName }} = {{ theme.color }}
{% endfor %}

body
	background $site-background-color
	color $text-defaut-color
